;0x00-0x07
add bl, [0xABCDABCD + 4 * eax]
add ebx, [0xABCDABCD + 4 * eax]
or bl, [0xABCDABCD + 4 * eax]
or ebx, [0xABCDABCD + 4 * eax]
adc bl, [0xABCDABCD + 4 * eax]
adc ebx, [0xABCDABCD + 4 * eax]
sbb bl, [0xABCDABCD + 4 * eax]
sbb ebx, [0xABCDABCD + 4 * eax]
and bl, [0xABCDABCD + 4 * eax]
and ebx, [0xABCDABCD + 4 * eax]
sub bl, [0xABCDABCD + 4 * eax]
sub ebx, [0xABCDABCD + 4 * eax]
xor bl, [0xABCDABCD + 4 * eax]
xor ebx, [0xABCDABCD + 4 * eax]
cmp bl, [0xABCDABCD + 4 * eax]
cmp ebx, [0xABCDABCD + 4 * eax]
bound eax, [0xABCDABCD + 4 * eax]
arpl [0xABCDABCD + 4 * eax], cx
test [0xABCDABCD + 4 * eax], dl
test [0xABCDABCD + 4 * eax], edx
xchg [0xABCDABCD + 4 * eax], dl
xchg [0xABCDABCD + 4 * eax], edx
mov [0xABCDABCD + 4 * eax], dl
mov [0xABCDABCD + 4 * eax], edx
mov dl, [0xABCDABCD + 4 * eax]
mov edx, [0xABCDABCD + 4 * eax]
mov [0xABCDABCD + 4 * eax], ds
lea edx, [0xABCDABCD + 4 * eax]
mov ds, [0xABCDABCD + 4 * eax]
mov al, [0xABCDABCD + 4 * eax]
mov eax, [0xABCDABCD + 4 * eax]
mov [0xABCDABCD + 4 * eax], al
mov [0xABCDABCD + 4 * eax], eax
les eax, [0xABCDABCD + 4 * eax]
lds eax, [0xABCDABCD + 4 * eax]
